/*this is tutorial for storage type in case.

type 1 is AUTOMATIC VARIABLES:
its scope is defined in local to the function body.
a variable which which is not defined specifically by default it goes into automatic.
ex: int harry and auto harry is same.

type 2 is EXTERNAL VARIABLES :
they are same as globle variable .

type 3 is STATIC VARIALBES :
we read it into ctuts 42.
in which the variable which conserve its value after running the program.

type 4 is REGISTER VARIABLES :
it request to compiler to store the variable in cpu instead of memory to access the program faster. */


